# DO NOT copy these placeholder values to Vercel.
# Use the real values from your local .env file instead.

DATABASE_URL="postgresql://REPLACE_WITH_YOUR_REAL_CONNECTION_STRING"
AUTH_SECRET="run: openssl rand -base64 32"
AUTH_URL="https://stock-ochre-mu.vercel.app"
